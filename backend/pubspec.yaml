name: sepet_backend
description: Sepet fiyat API — resmi market API / partner feed adaptörleri
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.6.2

dependencies:
  http: ^1.2.2
  shelf: ^1.4.2
  shelf_cors_headers: ^0.1.5
  shelf_router: ^1.1.4

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.8
